version: "3.9"
services:
  health-check:
    image: letsgetrusty/health-check
    build:
      context: .
      dockerfile: Dockerfile-health
    restart: "always"
    depends_on:
      auth:
        condition: service_started
    environment:
      - AUTH_SERVICE_HOST_NAME=auth
  auth:
    image: letsgetrusty/auth
    build:
      context: .
      dockerfile: Dockerfile-auth
    restart: "always"
    ports:
      - "50051:50051"